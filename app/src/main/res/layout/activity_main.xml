<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/fl_main"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"></FrameLayout>

    <RadioGroup
        android:id="@+id/rg_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/home_bottom_parent_bg"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_home"
            style="@style/MyRadioButton"
            android:drawableTop="@drawable/rb_drawable_home_selector"
            android:text="首页" />


        <RadioButton
            android:id="@+id/rb_fun"
            style="@style/MyRadioButton"
            android:drawableTop="@drawable/rb_drawable_community_selector"
            android:text="来呀" />


        <RadioButton
            android:id="@+id/rb_user"
            style="@style/MyRadioButton"
            android:drawableTop="@drawable/rb_drawable_user_selector"
            android:text="用户" />

    </RadioGroup>

</LinearLayout>
